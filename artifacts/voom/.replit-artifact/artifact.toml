kind = "web"
previewPath = "/"
title = "Voom - Car Rental"
version = "1.0.0"
id = "artifacts/voom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 24631
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/voom run dev"

[services.env]
PORT = "24631"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/voom", "run", "build"]
publicDir = "artifacts/voom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
